name: Deploy Mars to K3s

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      target:
        description: 'Deploy target'
        type: choice
        options:
          - all
          - web
          - server
        default: all

env:
  NAMESPACE: mars-system
  CLUSTER_NAME: mars-cluster

jobs:
  # ─── Job 1: 检测版本变更（仅 package.json version 变化触发发版） ───
  detect-version-changes:
    runs-on: self-hosted
    outputs:
      service: ${{ steps.version.outputs.service }}
      web: ${{ steps.version.outputs.web }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure git to use SSH (HTTPS blocked by firewall)
        run: |
          git config --global url."git@github.com:".insteadOf "https://github.com/"

      - name: Detect version changes
        id: version
        run: |
          BASE_SHA="${{ github.event.before }}"

          # 首次推送（无 base commit）→ 不发版
          if [ "$BASE_SHA" = "0000000000000000000000000000000000000000" ]; then
            echo "service=false" >> $GITHUB_OUTPUT
            echo "web=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          # 检查 @mars/server 版本
          SERVER_OLD=$(git show "$BASE_SHA:apps/server/package.json" 2>/dev/null | jq -r '.version')
          SERVER_NEW=$(jq -r '.version' apps/server/package.json 2>/dev/null)
          if [ "$SERVER_OLD" != "$SERVER_NEW" ] && [ -n "$SERVER_NEW" ]; then
            echo "service=true" >> $GITHUB_OUTPUT
            echo "Server: $SERVER_OLD → $SERVER_NEW"
          else
            echo "service=false" >> $GITHUB_OUTPUT
            echo "Server unchanged: $SERVER_NEW"
          fi

          # 检查 @mars/web 版本
          WEB_OLD=$(git show "$BASE_SHA:apps/web/package.json" 2>/dev/null | jq -r '.version')
          WEB_NEW=$(jq -r '.version' apps/web/package.json 2>/dev/null)
          if [ "$WEB_OLD" != "$WEB_NEW" ] && [ -n "$WEB_NEW" ]; then
            echo "web=true" >> $GITHUB_OUTPUT
            echo "Web: $WEB_OLD → $WEB_NEW"
          else
            echo "web=false" >> $GITHUB_OUTPUT
            echo "Web unchanged: $WEB_NEW"
          fi

      - name: Show detection result
        run: |
          echo "service: ${{ steps.version.outputs.service }}"
          echo "web: ${{ steps.version.outputs.web }}"

  # ─── Job 2: 发版后端（可选，与 web 并行） ───
  release-service:
    needs: detect-version-changes
    if: |
      needs.detect-version-changes.outputs.service == 'true' ||
      (github.event_name == 'workflow_dispatch' && (inputs.target == 'all' || inputs.target == 'server'))
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Configure git to use SSH
        run: |
          git config --global url."git@github.com:".insteadOf "https://github.com/"

      - name: Build Mars Server image
        run: |
          docker build -f docker/Dockerfile.server -t mars-server:latest .

      - name: Import image to k3d
        run: |
          k3d image import mars-server:latest -c ${{ env.CLUSTER_NAME }}

      - name: Rollout restart
        run: |
          kubectl rollout restart deployment/mars-server -n ${{ env.NAMESPACE }}
          kubectl rollout status deployment/mars-server -n ${{ env.NAMESPACE }} --timeout=120s

      - name: Health check
        run: |
          echo "=== Health check: Mars Server ==="
          for i in $(seq 1 12); do
            if curl -sf http://localhost:3000/api/business > /dev/null 2>&1; then
              echo "Mars Server OK"
              exit 0
            fi
            echo "Attempt $i/12: not ready yet..."
            sleep 5
          done
          echo "FAILED: Mars Server did not respond"
          exit 1

  # ─── Job 3: 发版前端（可选，与 service 并行） ───
  release-web:
    needs: detect-version-changes
    if: |
      needs.detect-version-changes.outputs.web == 'true' ||
      (github.event_name == 'workflow_dispatch' && (inputs.target == 'all' || inputs.target == 'web'))
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Configure git to use SSH
        run: |
          git config --global url."git@github.com:".insteadOf "https://github.com/"

      - name: Build Mars Web image
        run: |
          docker build -f docker/Dockerfile.web -t mars-web:latest .

      - name: Import image to k3d
        run: |
          k3d image import mars-web:latest -c ${{ env.CLUSTER_NAME }}

      - name: Rollout restart
        run: |
          kubectl rollout restart deployment/mars-web -n ${{ env.NAMESPACE }}
          kubectl rollout status deployment/mars-web -n ${{ env.NAMESPACE }} --timeout=120s

      - name: Health check
        run: |
          echo "=== Health check: Mars Web ==="
          for i in $(seq 1 12); do
            if curl -sf http://localhost:3000/ > /dev/null 2>&1; then
              echo "Mars Web OK"
              exit 0
            fi
            echo "Attempt $i/12: not ready yet..."
            sleep 5
          done
          echo "FAILED: Mars Web did not respond"
          exit 1

  # ─── Job 4: 发版总结 + 飞书通知 ───
  release-summary:
    needs: [detect-version-changes, release-service, release-web]
    if: always()
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Determine deploy result
        id: result
        run: |
          SERVICE_RESULT="${{ needs.release-service.result }}"
          WEB_RESULT="${{ needs.release-web.result }}"

          echo "Service result: $SERVICE_RESULT"
          echo "Web result: $WEB_RESULT"

          NEED_SERVICE="${{ needs.detect-version-changes.outputs.service }}"
          NEED_WEB="${{ needs.detect-version-changes.outputs.web }}"

          # 构建部署描述
          TARGETS=""
          if [ "$NEED_SERVICE" = "true" ] || [ "${{ inputs.target }}" = "server" ] || [ "${{ inputs.target }}" = "all" ]; then
            TARGETS="${TARGETS}server "
          fi
          if [ "$NEED_WEB" = "true" ] || [ "${{ inputs.target }}" = "web" ] || [ "${{ inputs.target }}" = "all" ]; then
            TARGETS="${TARGETS}web "
          fi
          if [ -z "$TARGETS" ] && [ "${{ github.event_name }}" = "push" ]; then
            TARGETS="(no changes detected)"
          elif [ -z "$TARGETS" ]; then
            TARGETS="manual: ${{ inputs.target }}"
          fi

          echo "targets=$TARGETS" >> $GITHUB_OUTPUT

          COMMIT=$(git log --oneline -1 | cut -d' ' -f1)
          echo "commit=$COMMIT" >> $GITHUB_OUTPUT

          OVERALL="success"
          if [ "$SERVICE_RESULT" = "failure" ] || [ "$SERVICE_RESULT" = "cancelled" ] || \
             [ "$WEB_RESULT" = "failure" ] || [ "$WEB_RESULT" = "cancelled" ]; then
            OVERALL="failure"
          fi
          echo "overall=$OVERALL" >> $GITHUB_OUTPUT
          echo "Commit: $COMMIT, Targets: $TARGETS, Result: $OVERALL"

      - name: Send Feishu notification
        if: needs.release-service.result != 'skipped' || needs.release-web.result != 'skipped'
        env:
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
          FEISHU_WEBHOOK_SECRET: ${{ secrets.FEISHU_WEBHOOK_SECRET }}
        run: |
          [ -z "$FEISHU_WEBHOOK_URL" ] && echo "FEISHU_WEBHOOK_URL 未配置，跳过通知" && exit 0

          RESULT="${{ steps.result.outputs.overall }}"
          TARGETS="${{ steps.result.outputs.targets }}"
          COMMIT="${{ steps.result.outputs.commit }}"
          SR="${{ needs.release-service.result }}"
          WR="${{ needs.release-web.result }}"

          if [ "$RESULT" = "success" ]; then
            COLOR="green"; STATUS_TEXT="全部成功"
          else
            COLOR="red"; STATUS_TEXT="有任务失败"
          fi

          [ "$SR" = "success" ] && S_ICON="✅" || S_ICON="❌"
          [ "$WR" = "success" ] && W_ICON="✅" || W_ICON="❌"
          [ -z "$SR" ] || [ "$SR" = "skipped" ] && S_LINE="后端: - (未变更)" || S_LINE="后端: ${S_ICON}"
          [ -z "$WR" ] || [ "$WR" = "skipped" ] && W_LINE="前端: - (未变更)" || W_LINE="前端: ${W_ICON}"
          DETAIL="${S_LINE}\n${W_LINE}"

          # 飞书签名验证
          TIMESTAMP=$(date +%s)
          KEY=$(printf '%s\n%s' "${TIMESTAMP}" "${FEISHU_WEBHOOK_SECRET}")
          SIGN=$(openssl dgst -sha256 -hmac "${KEY}" -binary < /dev/null | base64 -w 0)

          jq -n \
            --arg timestamp "$TIMESTAMP" \
            --arg sign "$SIGN" \
            --arg color "$COLOR" \
            --arg commit "$COMMIT" \
            --arg targets "$TARGETS" \
            --arg status "$STATUS_TEXT" \
            --arg detail "$DETAIL" \
            '{
              timestamp: $timestamp,
              sign: $sign,
              msg_type: "interactive",
              card: {
                config: { wide_screen_mode: true },
                header: { title: { tag: "plain_text", content: "🚀 Mars 部署通知" }, template: $color },
                elements: [
                  { tag: "div", text: { tag: "lark_md", content: ("**提交**: " + $commit + "\n**目标**: " + $targets + "\n**结果**: " + $status + "\n---\n" + $detail) } }
                ]
              }
            }' | curl -s -X POST -H "Content-Type: application/json" -d @- "$FEISHU_WEBHOOK_URL"
